---
description: REMI project conventions, architecture, and guardrails
globs: ["**/*.py", "**/*.yaml", "**/*.yml"]
alwaysApply: true
---

# REMI Conventions

## Tooling

- Always use `uv run` to execute Python — never `python` or `python3` directly
- Always use `uv add` to add dependencies — never `pip install`
- Never reference `.venv/` paths

## Error Handling — Zero Silent Swallows

Every exception must either **propagate** or be **logged at warning+ with
`exc_info=True`**. There are no other options.

**Banned patterns** (will be rejected in review):
- `except Exception: pass`
- `except Exception: continue`
- `except SomeError: return None` without a log line
- `except Exception as e: return {"error": str(e)}` without logging
- Bare `except:` (always name the exception type)

**Acceptable patterns:**
- `except ImportError` for optional-dependency guards — must `raise RuntimeError` with install instructions or be an explicit feature-flag check
- `except (KeyboardInterrupt, EOFError)` in CLI interactive loops
- `except Exception` that logs at `warning`/`error` with `exc_info=True` **and** the caller can see the failure (via return type, error list, or re-raise)

**Rules for catch-and-continue (pipelines, producers, batch loops):**
- Log at `warning` with `exc_info=True`
- Track the failure in a result object (`errors` count, `failed_items` list, `pipeline_warnings`)
- The caller must be able to distinguish "nothing happened" from "it crashed"

**Use `structlog`** for all logging:
```python
import structlog
logger = structlog.get_logger(__name__)
```
Do not use `print()` or the stdlib `logging` module directly.

## Type Safety — No `Any`, No `dict`

**Banned in new code:**
- `dict[str, Any]` as a return type or function parameter — define a Pydantic model or TypedDict
- `-> Any` as a return type — be specific
- `list[dict]` or `list[dict[str, Any]]` — define a model for the items
- `Optional[X]` where `None` means "something went wrong" — raise instead

**Acceptable uses of `Any`:**
- `**kwargs: Any` in delegation/adapter boundaries
- JSON deserialization intermediaries (`json.loads` result) before immediate validation
- Third-party library interop where the upstream type is genuinely untyped

**When touching existing code** that uses `dict[str, Any]` or `Any`, narrow
the type if you can do so without a large refactor. At minimum, don't make
it worse.

## Import Hygiene

- Never use `typing.TYPE_CHECKING`. If you need it, the dependency is
  in the wrong place — fix the design, not the import.
- Keep imports direct and honest. If module A uses a type from module B
  at runtime, import it at the top of the file.

## Tests

- Only write tests for meaningful behavior — not implementation details
- Tests should verify real failure modes and edge cases
- Do not write tests that just confirm a function returns without crashing
- Ask before writing tests if the value isn't obvious

## Architecture: Two Halves + Shell

Full architecture rationale: `docs/architecture.md`

The codebase has two halves and a composition root. The directory tree
is the primary documentation. The generic/RE boundary is visible in `ls`.

```
src/remi/

  # ── Domain-agnostic capabilities ───────────────────────────
  # Each is self-contained. Could power any domain.
  
  types/       Pure vocabulary — ids, clock, errors, enums, result, text
  db/          Database engine + table metadata
  llm/         LLM provider ports + adapters (OpenAI, Anthropic, Gemini)
  vectors/     Embedding ports + adapters, pipeline
  sandbox/     Code execution ports + adapters (local, Docker)
  observe/     Structured logging (structlog), tracing
  signals/     Signal types, stores, engine, composite
  graph/       Knowledge graph types, bridge, retriever
  documents/   Document types, stores, parsers
  agent/       LLM runtime: loop, node, context builder, perception, tools

  # ── Real estate (the product) ──────────────────────────────
  # Replace this section to build a different product.
  
  portfolio/   Entity DTOs + protocols + business rules (~3 files)
  stores/      RE persistence adapters (mem, pg, rollups)
  queries/     RE query services (dashboard, rent roll, leases, etc.)
  evaluators/  RE signal producers (delinquency, lease, maintenance, etc.)
  ingestion/   RE inbound data pipeline (parse → extract → persist → enrich)
  ontology/    RE knowledge graph schema + remote client
  search/      RE-aware hybrid search + pattern detection
  tools/       RE agent capabilities (conversational agent tools)
  configs/     RE agent YAML manifests (director, researcher, etc.)

  # ── Composition root ───────────────────────────────────────
  
  shell/       DI container, settings, domain.yaml, API mountpoint, CLI
```

**State the intended package before creating a new file or adding significant logic.**

**Hexagonal boundary:** Generic code imports ports (ABCs/types.py) only —
never `psycopg2`, `openai`, `anthropic`, `docker`, etc. directly.
`shell/config/container.py` is the *only* file that knows both sides.

**Agent-first discoverability:** File paths, API routes, and module names are
part of the domain vocabulary. An LLM navigating this codebase must be able to
infer what exists from the directory tree alone.

**Two agent modes — know which you are building for:**
- *Code-first agents* run inside `sandbox/` as isolated subprocesses.
  They call REMI through `remi_data` (a stdlib HTTP bridge injected at session
  start). Their tool registry IS the API.
- *Conversational agents* run inside the REMI process. They call domain services
  through `tools/`. Their tool registry is `tools/__init__.py`.

## Repository Protocols

Port interfaces for storage live in `portfolio/protocols.py` as
**narrow, per-entity protocols** (e.g. `LeaseRepository`, `UnitRepository`).

- **New code must depend on the narrowest protocol** that covers its needs.
- When touching existing code that takes a composite store, narrow the type
  if the function only uses one or two entity groups.
- Mutating methods return `WriteResult[Entity]` with a `WriteOutcome`
  discriminator (`CREATED`, `UPDATED`, `NOOP`).

## Boundary Model Discipline

- **`queries/` owns read-models** (e.g. `RentRollResult`, `PortfolioOverview`).
- **`shell/api/` schemas re-export** from queries — no duplicate Pydantic classes.
  Only HTTP-specific types (request bodies, envelope wrappers) are defined in `shell/api/`.
- **CLI delegates** to queries/services and renders the result — no inline store queries.
- **No `SomeApiType(**service_result.model_dump())`** between identical types.

## Container Discipline

`shell/config/container.py` is pure wiring — no business logic, no factory decisions.

- Only attributes read **outside** `container.py` may be `self.*`.
- Before adding a new `self.*` attribute, verify it will be read externally.
- Factory functions belong in the module that owns the thing being built.

## Factory Pattern

Each module owns the factory for the thing it defines:

- `signals/composite.py` → `build_signal_pipeline()`
- `graph/bridge.py` → `build_knowledge_graph()`
- `agent/context_builder.py` → `build_context_builder()`
- `stores/factory.py` → `build_property_store()`, `build_rollup_store()`
- `llm/factory.py` → `build_provider_factory()`
- `vectors/embedder.py` → `build_embedder()`
- `sandbox/factory.py` → `build_sandbox()`

`container.py` calls these — it does not inline assembly.

## Key Constraints

- `shell/config/domain.yaml` is the source of truth for signal definitions,
  thresholds, and rules — do not hardcode these values in Python
- The LLM agent does NOT detect signals — the entailment engine does.
  The LLM explains, connects, recommends, and codifies.
- `types/` is for cross-cutting primitives only — no business logic
- Prefer small focused modules over accumulating logic in an existing file
- When a file is growing beyond a single clear responsibility, flag it

## Module Map

```
src/remi/

── types/ ───────────────────────────────────────────────────────────────────
  ids, clock, errors, enums, result, text

── db/ ──────────────────────────────────────────────────────────────────────
  engine, tables

── llm/ ─────────────────────────────────────────────────────────────────────
  types (ports), openai, anthropic, gemini, factory

── vectors/ ─────────────────────────────────────────────────────────────────
  types (ports), embedder, store, pipeline, tokens

── sandbox/ ─────────────────────────────────────────────────────────────────
  types (ports), local, docker, policy, bridge, factory

── observe/ ─────────────────────────────────────────────────────────────────
  types, logging, tracer, mem

── signals/ ─────────────────────────────────────────────────────────────────
  types (Signal, SignalDefinition, DomainRulebook, Policy)
  stores (SignalStore, FeedbackStore, HypothesisStore ABCs)
  engine (generic dispatch), composite, mem

── graph/ ───────────────────────────────────────────────────────────────────
  types (KnowledgeGraph, Entity, Relationship ABCs)
  bridge, retriever, mem

── documents/ ───────────────────────────────────────────────────────────────
  types (Document, DocumentStore ABCs), parsers, mem, pg

── agent/ ───────────────────────────────────────────────────────────────────
  types (ToolRegistry, ToolDefinition, Message, ChatSession ABCs)
  runner, loop, node, llm_bridge, tool_executor, thread
  context_builder (perception), context_rendering, context
  intent, config, retry, base, registry, mem

── portfolio/ ───────────────────────────────────────────────────────────────
  models (Property, Unit, Tenant, Lease, Manager, Portfolio DTOs)
  protocols (narrow repo ABCs)
  rules (occupancy, vacancy, loss-to-lease helpers)

── stores/ ──────────────────────────────────────────────────────────────────
  mem, pg, rollups, factory

── queries/ ─────────────────────────────────────────────────────────────────
  dashboard, rent_roll, leases, properties, portfolios, maintenance
  managers, snapshots, metrics, auto_assign

── evaluators/ ──────────────────────────────────────────────────────────────
  delinquency, existence, lease, maintenance, portfolio
  threshold, trend, composition, statistical, base

── ingestion/ ───────────────────────────────────────────────────────────────
  pipeline, service, engine, seed, enrichment, llm_adapters
  managers, generic, adapters/ (appfolio/, registry)

── ontology/ ────────────────────────────────────────────────────────────────
  schema, remote

── search/ ──────────────────────────────────────────────────────────────────
  service, pattern, graduation

── tools/ ───────────────────────────────────────────────────────────────────
  __init__ (register_all_tools), ontology, documents, search, vectors
  sandbox, actions, memory, trace, http, workflows, delegation, snapshots

── configs/ ─────────────────────────────────────────────────────────────────
  director/, researcher/, knowledge_enricher/, report_classifier/,
  action_planner/

── shell/ ───────────────────────────────────────────────────────────────────
  config/   container, settings, domain.yaml
  api/      main (mountpoint), dependencies, middleware, error_handler,
            schemas, + all route modules
  cli/      main (mountpoint), shared, banner, live_display, http,
            + all CLI modules
```
