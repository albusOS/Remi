"""Core — internal plumbing for the RE domain.

Entity models and repository protocols (``portfolio/``), persistence
adapters (``stores/``), and knowledge-graph schema (``ontology/``).
Not directly invoked by agents or CLI — consumed by contexts.
"""
