"""Agent — all AI infrastructure.

Owns the LLM runtime, execution engine, context building, perception,
conversation management, and every subsystem the agent needs: LLM
providers, embeddings, sandbox, observability, signals, knowledge graph,
documents, and database.
"""
