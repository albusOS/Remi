"""Configuration types — DEPRECATED.

Product-level settings (``RemiSettings``, ``ApiSettings``, etc.) have moved
to ``remi.shell.config.settings`` where they belong — the composition root.
Kernel infrastructure settings (LLM, sandbox, embeddings, etc.) live in
``remi.agent.config``.

This module is intentionally empty. Do not add types here.
"""
