"""Infrastructure — port implementations for external systems.

stores/         PropertyStore implementations (in-memory, Postgres)
graph/          RE WorldModel wrapping PropertyStore with FK-derived links
"""
